spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/vocabcheck
    username: vocabcheck
    password: vocabcheck
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
    init:
      mode: never

vocabcheck:
  words:
    url: http://localhost:8080/